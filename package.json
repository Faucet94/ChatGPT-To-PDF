{
  "name": "html-to-pdf-engine",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/shared -w packages/core -w packages/queue -w packages/renderer -w packages/storage -w packages/templates && npm run build -w apps/api -w apps/worker",
    "dev:api": "npm run dev -w apps/api",
    "dev:worker": "npm run dev -w apps/worker",
    "start:api": "npm run start -w apps/api",
    "start:worker": "npm run start -w apps/worker"
  },
  "devDependencies": {
    "@types/node": "^25.8.0",
    "typescript": "^6.0.3"
  }
}
